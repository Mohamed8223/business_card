enum AccontType { admin, doctor, user, marketer }

enum FollowingsFollowersTaps { followings, followers }

enum Gender { male, female }

enum AuthStatus { auth, notAuth, init }

enum LoadingTypes {
  daialog,
  inline,
}

enum AttatchmentType { pdf, image, none }

enum ChatMessageType { text, image, pdf }

enum CategorySectionLayout { horizontal, vertical }

enum DoctorJob { dentist, beauty, dentistAndBeauty }

enum ClinicJob { dentist, beauty, dentistAndBeauty }

enum CategoryType {
  specialist,
  service,
}
